<h1>EditingBook</h1>

<%= form_with model:@book, url:"book_path(book.id)", local:true do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 <h4>Body</h4>
 <%= f.text_area :body %>
 <%= f.submit 'Update Book' %>
<% end %>
<!--show url check-->
<%= link_to "Show", books_index_path%>
<%= link_to "back", books_index_path %>
